# Stage that builds the application, a prerequisite for the running stage
FROM maven:3-openjdk-11-slim as build
RUN curl -sL https://deb.nodesource.com/setup_14.x | bash -
RUN apt-get update -qq && apt-get install -qq --no-install-recommends nodejs

ARG JAVA_OPTS
ENV JAVA_OPTS=$JAVA_OPTS

# Stop running as root at this point
RUN useradd -m chef
WORKDIR /usr/src/app/
RUN chown chef:chef /usr/src/app/
USER chef

# Copy pom.xml and prefetch dependencies so a repeated build can continue from the next step with existing dependencies
COPY --chown=chef pom.xml ./
RUN mvn dependency:go-offline -Pproduction

# Copy all needed project files to a folder
COPY --chown=chef:chef src src
COPY --chown=chef:chef frontend frontend
COPY --chown=chef:chef package.json ./

# Using * after the files that are autogenerated so that so build won't fail if they are not yet created
COPY --chown=chef:chef package-lock.json* pnpm-lock.yaml* webpack.config.js* ./


# Build the production package, assuming that we validated the version before so no need for running tests again
RUN mvn clean package -DskipTests $JAVA_OPTS -Djava.security.egd=file:/dev/./urandom -Pproduction

# Running stage: the part that is used for running the application
FROM openjdk:11-jdk-slim
COPY --from=build /usr/src/app/target/*.jar /usr/app/app.jar
RUN useradd -m chef
USER chef
EXPOSE 8080
CMD java -jar /usr/app/app.jar
