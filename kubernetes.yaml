apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkinout
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: checkinout
  template:
    metadata:
      labels:
        app: checkinout
    spec:
      containers:
        - name: checkinout
          image: checkinout:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: 512Mi
              cpu: '1'

---
apiVersion: v1
kind: Service
metadata:
  name: checkinout-balancer
spec:
  selector:
    app: checkinout
  ports:
    - protocol: TCP
      port: 8000 # The port to expose to the outside
      targetPort: 8080 # The port the application is running on in the pods
  type: LoadBalancer
  sessionAffinity: ClientIP
